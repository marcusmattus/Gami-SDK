// Import from shared schema for consistency
import { partners, insertPartnerSchema, Partner, InsertPartner } from '@shared/schema';

// Re-export the schema elements for easy access
export { partners, insertPartnerSchema, Partner, InsertPartner };