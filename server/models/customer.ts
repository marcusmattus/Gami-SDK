// Import from shared schema for consistency
import { customers, insertCustomerSchema, Customer, InsertCustomer } from '@shared/schema';

// Re-export the schema elements for easy access
export { customers, insertCustomerSchema, Customer, InsertCustomer };